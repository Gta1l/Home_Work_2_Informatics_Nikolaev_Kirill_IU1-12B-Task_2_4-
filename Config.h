#pragma once
#include <string>
std::string dbname = "DWH";
std::string user = "postgres";
std::string password = "7341";
std::string host = "localhost";
std::string port = "5432";

std::string config = "dbname = " + dbname + " user = " + user + " password = " + password + " host = " + host + " port = " + port;
